require('dotenv').config(); const bcrypt = require('bcryptjs'); const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); async function main() { const email = 'admin@youthcamping.online'; const plainPassword = 'admin@123456'; const hashed = await bcrypt.hash(plainPassword, 10); await prisma.admin.update({ where: { email }, data: { password: hashed } }); console.log('Admin password hashed successfully'); } main().catch(console.error).finally(() => prisma.$disconnect());
